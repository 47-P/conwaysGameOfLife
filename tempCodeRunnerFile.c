// Changing the grid everytime we run the loop so that we can store the new values
        // for (int i = 0; i < maxRow; maxRow++)
        // {
        //     for (int j = 0; j < maxColumn; maxColumn++)
        //     {
        //         if (gridSelector % 2 == 0)
        //         {
        //             grid[i][j] = grid1[i][j];
        //         }

        //         else
        //         {
        //             grid[i][j] = grid2[i][j];
        //         }
        //     }
        // }